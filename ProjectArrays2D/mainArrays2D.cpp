#include<iostream>
using namespace std;

void main()
{
	setlocale(LC_ALL, "Russin");

	const int ROWS = 8;//Количество строк
	const int COLS = 5;//Количество столбиков (элементов строки)
	int arr[ROWS][COLS] =
	{
		{},
		{1,2,5,8,2},
		{3,24,1}
	};

	for (int i = 0; i < ROWS; i++)//Счетчик i нумерует строки
	{
		for (int j = 0; j < COLS; j++)//Счетчик j нумерует столбики (элементы строки)
		{

		}
	}
}